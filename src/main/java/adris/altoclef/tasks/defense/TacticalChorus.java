package adris.altoclef.tasks.defense;

import adris.altoclef.AltoClef;

public class TacticalChorus {

    public static boolean attemptTp(final AltoClef mod) {
        /*

         */
        //mod.getClientBaritone().getWorldProvider().getCurrentWorld().getCachedWorld().getRegion(0, 0).getBlock()
        //mod.getPlayer().raycast(5, 1, false);
       // mod.getPlayer().getBoundingBox().raycast()
        return false;
    }
}
